import org.scalatest.flatspec.AnyFlatSpec

class AnagramCheckerSpec extends AnyFlatSpec{

  "For a given word, a call to getAnagramsForWord " should "return all anagrams for it from words text file" in {
    val word = "Abbe";
//    assert(AnagramChecker.getAnagramsForWord(word).length == 2)
  }



}
